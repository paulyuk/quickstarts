# yaml-language-server: $schema=https://azuresdkreleasepreview.blob.core.windows.net/azd/schema/azure.yaml.json

name: pubsub-dapr-nodejs-servicebus
metadata:
  template: pubsub-dapr-nodejs-servicebus@0.0.1-beta
services:
  checkout:
    project: checkout
    language: js
    host: containerapp
  orders:
    project: order-processor
    language: js
    host: containerapp